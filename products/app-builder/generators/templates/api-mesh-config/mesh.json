{
  "meshConfig": {
    "sources": [
      {
        "name": "${sourceName}",
        "handler": {
          "graphql": {
            "endpoint": "${endpoint}"
          }
        }
      }
    ],
    "transforms": []
  }
}
